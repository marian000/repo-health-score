# Planted Fixture

A small billing library, carrying one of every problem the scanner looks for.

This file deliberately stops here. It has an Installation section and no Usage
section, which is half of what the documentation module checks for.

## Installation

```
npm install planted-fixture
```
