APP_ENV=production
APP_DEBUG=false

# Substituted by tests/support/fixtures.ts when the fixture is materialised.
# The real credential-shaped string is never committed: this project's own
# secret scan would find it, and GitHub push protection would reject the push.
AWS_ACCESS_KEY_ID=__PLANTED_AWS_ACCESS_KEY__
AWS_DEFAULT_REGION=eu-west-1
